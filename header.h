#include <fstream> 
#include <iostream>
#include <cstring> 
#include <stdlib.h>
#include <time.h>
using namespace std;
int edl(char* path,char* limit,char* mult);
int man();
int eMl(char* limit,char* mult);
